<!-- convex-ai-start -->
This project uses [Convex](https://convex.dev) as its backend.

When working on Convex code, **always read `convex/_generated/ai/guidelines.md` first** if it exists for important guidelines on how to correctly use Convex APIs and patterns.

Always use `npx convex dev` during development. Only use `npx convex deploy` for production.
<!-- convex-ai-end -->
